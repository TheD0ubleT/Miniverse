package sim;

// sometimes I believe compiler ignores all my comments
// total_hours_wasted_fixing_nothing = 2;

public final class ForTheLolz
{
	
	//Crashed program (due to StackOverflow)
	//Because why not
	public final static int crash()
	{
		return crash();
	}
	
	//returns 1, for some reason
	public int return1()
	{
		return (byte) + (char) - (int) + (long) - 1;
	}
}
